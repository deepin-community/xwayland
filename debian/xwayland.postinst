#!/bin/sh
set -e

if [ "$1" = "configure" ] && command -v systemctl >/dev/null 2>&1; then
    systemctl --global enable xwayland-screencast-helper.socket >/dev/null 2>&1 || true
fi

#DEBHELPER#

exit 0
